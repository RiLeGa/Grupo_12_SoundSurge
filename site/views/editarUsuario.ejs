<!DOCTYPE html>
<html lang="es">
    <%- include ("./partials/head",{title: "editarPerfil"}) %>
<body class="register">
    <main>
        <section class="container-register">
            <h1>Registro</h1>
            <p>Completa el registro con lo solicitado<br>para tener la mejor experiencia en Sound<br>Surge.</p>

            <form action="/user/editar/<%= locals.userLogin.id %>?_method=put" method="post" >
            <div class="icons">
                <i class="fas fa-user"> <input type="file" name="imagen" id="imagen"> </i>
            </div>
            <div class="container">
                <label for="nombre">nombre</label>
                <input type="text" name="nombre" id="nombre" placeholder="Ingresa tu nombre" value="">
                <% if (locals.errors && errors.nombre) { %>
                    <small><%= errors.nombre.msg %></small>
                <% } %>
            </div>
            <div class="container">
                <label for="apellido">apellido</label>
                <input type="text" name="apellido" id="apellido" placeholder="Ingresa tu apellido">
                <% if (locals.errors && errors.apellido) { %>
                    <small><%= errors.apellido.msg %></small>
                <% } %>
            </div>
            <div class="container">
                <label for="direccion">direccion</label>
                <input type="text" name="direccion" id="direccion" placeholder="Ingresa tu direccion">
                <% if (locals.errors && errors.direccion) { %>
                    <small><%= errors.direccion.msg %></small>
                <% } %>
            </div>
            <div class="container">
                <label for="telefono">telefono</label>
                <input type="text" name="telefono" id="telefono" placeholder="Ingresa tu telefono">
                <% if (locals.errors && errors.telefono) { %>
                    <small><%= errors.telefono.msg %></small>
                <% } %>
            </div>
            <div class="container">
                <label for="email">email</label>
                <input type="email" name="email" id="email" placeholder="Ingresa tu email">
                <% if (locals.errors && errors.email) { %>
                    <small><%= errors.email.msg %></small>
                <% } %>
            </div>
            <div class="container">
                <label for="contrasenia">contrasenia</label>
                <input type="password" name="contrasenia" id="contrasenia" placeholder="Ingresa tu contraseña">
                <% if (locals.errors && errors.contrasenia) { %>
                    <small><%= errors.contrasenia.msg %></small>
                <% } %>
            </div>
            <div class="container">
                <label for="confirmar">Confirmar contraseña</label>
                <input type="password" name="confirmar" id="confirmar" placeholder="Confirma tu contraseña">
                <% if (locals.errors && errors.confirmar) { %>
                    <small><%= errors.confirmar.msg %></small>
                <% } %>
            </div>
           

            <div class="terminos">
                <input type="checkbox" name="terminos" id="terminos">
                <% if (locals.errors && errors.terminos) { %>
                    <small><%= errors.terminos.msg %></small>
                <% } %>
                <label for="terminos">Acepto los terminos y condiciones</label>
            </div>
            
            <button type="submit" class="enviar">Enviar</button>
            
        </form>
        <div class="foot-reg">
            <a href="/">Soporte</a>
            <a href="/">Home</a>
            <a href="/">Contacto</a>
        </div>
    </section>                  
    </main>
</body>
</html>